apiVersion: v1
kind: ServiceAccount
metadata:
  name: invoice-mgr-app-sa
  labels:
    account: invoice-mgr-app
    app: invoice-mgr-app
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: invoice-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: invoice-mgr-app
  template:
    metadata:
      labels:
        app: invoice-mgr-app
    spec:
      serviceAccountName: invoice-mgr-app-sa
      containers:
        - name: invoice-mgr-app
          image: invoice-mgr-app:v1.0.0
          ports:
            - containerPort: 3000
          imagePullPolicy: Never
          env:
            - name: GENERATOR_URL
              value: "http://invoice-mgr-gen-svc:3100"
            - name: DATA_URL
              value: "http://invoice-mgr-data-svc:3300"
            - name: PAYMENT_URL
              value: "http://invoice-mgr-payment-svc:3200"
---

apiVersion: v1
kind: Service
metadata:
  name: invoice-mgr-app-svc
spec:
  selector:
    app: invoice-mgr-app
  ports:
    - port: 3000
      targetPort: 3000