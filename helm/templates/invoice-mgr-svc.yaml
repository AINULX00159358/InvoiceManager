apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.name }}-svc-{{ .Values.app.version }}
  namespace: {{.Release.Namespace}}
spec:
  selector:
    app: {{ .Values.app.name }}
    version: {{ .Values.app.version }}
  ports:
    - name: http
      port: {{ .Values.app.service.port }}
      targetPort: 3000