apiVersion: apps/v1
kind: Deployment
metadata:
  name: invoice-data
  namespace: {{.Release.Namespace}}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: invoice-mgr-data
  template:
    metadata:
      labels:
        app: invoice-mgr-data
        version: 1.0.0
    spec:
      serviceAccountName: invoice-mgr-sa
      containers:
        - name: invoice-mgr-data
          image: invoice-mgr:v1.0.0
          command: [ "npm" ]
          args: [ "run", "data" ]
          ports:
            - containerPort: 3000
          imagePullPolicy: Never
          env:
            - name: INVOICE_URL
              value: "http://invoice-mgr-gen-svc:3100"
            - name: DATA_URL
              value: "http://invoice-mgr-data-svc:3300"
            - name: PAYMENT_URL
              value: "http://invoice-mgr-payment-svc:3200"
---
apiVersion: v1
kind: Service
metadata:
  name: invoice-mgr-data-svc
  namespace: {{.Release.Namespace}}
spec:
  selector:
    app: invoice-mgr-data
  ports:
    - name: http-invoice-mgr-data
      port: 3300
      targetPort: 3000
