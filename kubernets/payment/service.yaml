apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: default
  labels:
    app: payment
    version: v1
spec:
  selector:
    app: payment
    version: v1
  ports:
    - name: http
      port: 3200
      targetPort: 3000
