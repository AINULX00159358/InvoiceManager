apiVersion: v1
kind: Service
metadata:
  name: data
  namespace: default
  labels:
    app: data
    version: v1
spec:
  selector:
    app: data
    version: v1
  ports:
    - name: http
      port: 3300
      targetPort: 3000
